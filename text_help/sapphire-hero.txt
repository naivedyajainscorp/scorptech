.s-stage{
  display:flex;
  flex-direction:column;
  gap:0;
  align-items:center;
  position:relative;
}

.s-pipeline{
  width:100%;
  max-width:600px;
  background:linear-gradient(180deg,rgba(255,255,255,.07),rgba(255,255,255,.03));
  border:1px solid rgba(255,255,255,.1);
  border-radius:28px;
  padding:28px 22px 22px;
  box-shadow:0 28px 72px rgba(0,0,0,.38);
  backdrop-filter:blur(20px);
  position:relative;
  overflow:hidden;
}

.s-pipeline::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(135deg,rgba(78,161,255,.05),transparent 50%,rgba(124,92,255,.05));
  pointer-events:none;
}

.s-pipe-label{
  text-align:center;
  font-size:.72rem;
  letter-spacing:.14em;
  text-transform:uppercase;
  color:rgba(238,244,255,.4);
  margin-bottom:20px;
}

.s-flow{
  display:grid;
  grid-template-columns:1fr auto 1fr;
  gap:14px;
  align-items:center;
}

.s-col-label{
  font-size:.7rem;
  letter-spacing:.1em;
  text-transform:uppercase;
  color:rgba(238,244,255,.35);
  text-align:center;
  margin-bottom:10px;
}

.s-in-col,
.s-out-col{
  display:flex;
  flex-direction:column;
  gap:9px;
}

.s-chaos-card{
  border-radius:14px;
  padding:10px 12px;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.08);
  position:relative;
  overflow:hidden;
  opacity:0;
  animation:s-fadeUp .5s ease forwards;
}

.s-chaos-card::before{
  content:"";
  position:absolute;
  left:0;
  top:0;
  bottom:0;
  width:3px;
  border-radius:3px 0 0 3px;
}

.s-chaos-card.s-form::before{background:#f4b740;}
.s-chaos-card.s-event::before{background:#4ea1ff;}
.s-chaos-card.s-doc::before{background:#7c5cff;}
.s-chaos-card.s-email::before{background:#ff6b6b;}

.s-chaos-card .s-ct{
  font-size:.72rem;
  font-weight:600;
  color:rgba(238,244,255,.5);
  letter-spacing:.08em;
  text-transform:uppercase;
  margin-bottom:3px;
}

.s-chaos-card .s-cv{
  font-size:.82rem;
  color:rgba(238,244,255,.82);
  line-height:1.35;
}

.s-chaos-card .s-lines{
  margin-top:6px;
  display:flex;
  flex-direction:column;
  gap:3px;
}

.s-chaos-card .s-ln{
  height:4px;
  border-radius:999px;
  background:rgba(255,255,255,.09);
}

.s-chaos-card .s-ln:nth-child(1){width:85%;}
.s-chaos-card .s-ln:nth-child(2){width:65%;}
.s-chaos-card .s-ln:nth-child(3){width:72%;}

.s-core-col{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:10px;
}

.s-arrow{
  display:flex;
  align-items:center;
  justify-content:center;
  flex-direction:column;
  gap:6px;
}

.s-arr-track{
  width:2px;
  height:60px;
  background:linear-gradient(180deg,rgba(78,161,255,.08),rgba(78,161,255,.55),rgba(124,92,255,.55),rgba(124,92,255,.08));
  border-radius:999px;
  position:relative;
  overflow:hidden;
}

.s-arr-track::after{
  content:"";
  position:absolute;
  left:0;
  right:0;
  height:28px;
  background:linear-gradient(180deg,transparent,rgba(255,255,255,.9),transparent);
  animation:s-slide 1.4s linear infinite;
}

.s-arr-chevrons{
  display:flex;
  flex-direction:column;
  align-items:center;
  gap:2px;
}

.s-arr-chevrons span{
  display:block;
  width:8px;
  height:8px;
  border-right:1.5px solid rgba(78,161,255,.55);
  border-bottom:1.5px solid rgba(78,161,255,.55);
  transform:rotate(45deg);
  animation:s-chevPulse 1.4s ease infinite;
}

.s-arr-chevrons span:nth-child(2){
  animation-delay:.18s;
  opacity:.72;
}

.s-arr-chevrons span:nth-child(3){
  animation-delay:.36s;
  opacity:.44;
}

.s-core-orb{
  width:72px;
  height:72px;
  border-radius:50%;
  background:radial-gradient(circle at 38% 32%,rgba(255,255,255,.85),rgba(78,161,255,.3) 45%,rgba(124,92,255,.2) 75%,transparent 100%);
  box-shadow:0 0 0 10px rgba(78,161,255,.07),0 0 0 22px rgba(124,92,255,.04),0 0 36px rgba(78,161,255,.2);
  position:relative;
  flex-shrink:0;
}

.s-core-orb::before{
  content:"";
  position:absolute;
  inset:8px;
  border-radius:50%;
  border:1px solid rgba(255,255,255,.12);
  animation:s-orbitSpin 3.5s linear infinite;
}

.s-core-orb::after{
  content:"";
  position:absolute;
  inset:18px;
  border-radius:50%;
  background:rgba(255,255,255,.06);
  border:1px solid rgba(255,255,255,.08);
}

.s-core-tag{
  font-size:.62rem;
  letter-spacing:.14em;
  text-transform:uppercase;
  color:#4ea1ff;
  font-weight:700;
  text-align:center;
}

.s-out-card{
  border-radius:14px;
  padding:10px 12px;
  background:linear-gradient(135deg,rgba(35,193,139,.1),rgba(35,193,139,.04));
  border:1px solid rgba(35,193,139,.2);
  opacity:0;
  animation:s-fadeUp .5s ease forwards;
}

.s-out-card .s-ot{
  font-size:.7rem;
  font-weight:700;
  color:#23c18b;
  letter-spacing:.08em;
  text-transform:uppercase;
  margin-bottom:3px;
}

.s-out-card .s-ov{
  font-size:.82rem;
  color:rgba(238,244,255,.85);
  line-height:1.3;
}

.s-out-card .s-tag-row{
  display:flex;
  flex-wrap:wrap;
  gap:4px;
  margin-top:6px;
}

.s-out-card .s-tag{
  padding:2px 7px;
  border-radius:999px;
  background:rgba(35,193,139,.12);
  border:1px solid rgba(35,193,139,.22);
  font-size:.68rem;
  color:#23c18b;
}

.s-out-card.s-event-out{
  background:linear-gradient(135deg,rgba(78,161,255,.1),rgba(78,161,255,.04));
  border-color:rgba(78,161,255,.22);
}

.s-out-card.s-event-out .s-ot{color:#4ea1ff;}
.s-out-card.s-event-out .s-tag{
  background:rgba(78,161,255,.12);
  border-color:rgba(78,161,255,.22);
  color:#4ea1ff;
}

.s-out-card.s-doc-out{
  background:linear-gradient(135deg,rgba(124,92,255,.1),rgba(124,92,255,.04));
  border-color:rgba(124,92,255,.22);
}

.s-out-card.s-doc-out .s-ot{color:#b39dff;}
.s-out-card.s-doc-out .s-tag{
  background:rgba(124,92,255,.12);
  border-color:rgba(124,92,255,.22);
  color:#b39dff;
}

.s-status-bar{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:14px;
  margin-top:16px;
  padding:10px 16px;
  border-radius:14px;
  background:rgba(255,255,255,.04);
  border:1px solid rgba(255,255,255,.07);
}

.s-status-dot{
  width:7px;
  height:7px;
  border-radius:50%;
  background:#23c18b;
  box-shadow:0 0 0 4px rgba(35,193,139,.12);
  flex-shrink:0;
  animation:s-pulse 2s ease infinite;
}

.s-status-text{
  font-size:.78rem;
  color:rgba(238,244,255,.6);
}

.s-status-text strong{
  color:rgba(238,244,255,.9);
  font-weight:600;
}

.s-metrics{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:9px;
  margin-top:12px;
  width:100%;
  max-width:600px;
}

.s-metric{
  border-radius:14px;
  padding:12px 14px;
  background:rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.08);
  text-align:center;
}

.s-metric .s-mn{
  font-size:1.4rem;
  font-weight:800;
  letter-spacing:-.03em;
  background:linear-gradient(135deg,#fff,#a0c8ff);
  -webkit-background-clip:text;
  background-clip:text;
  color:transparent;
}

.s-metric .s-ml{
  font-size:.7rem;
  color:rgba(238,244,255,.45);
  letter-spacing:.06em;
  text-transform:uppercase;
  margin-top:2px;
}

@keyframes s-fadeUp{
  from{opacity:0;transform:translateY(12px);}
  to{opacity:1;transform:translateY(0);}
}

@keyframes s-slide{
  from{top:-28px;}
  to{top:100%;}
}

@keyframes s-chevPulse{
  0%,100%{opacity:.3;}
  50%{opacity:1;}
}

@keyframes s-orbitSpin{
  from{transform:rotate(0deg) scaleX(1.4);}
  to{transform:rotate(360deg) scaleX(1.4);}
}

@keyframes s-pulse{
  0%,100%{box-shadow:0 0 0 4px rgba(35,193,139,.12);}
  50%{box-shadow:0 0 0 8px rgba(35,193,139,.05);}
}

@media(max-width:640px){
  .s-pipeline{padding:20px 14px 16px;}
  .s-flow{gap:8px;}
  .s-core-orb{width:56px;height:56px;}
  .s-arr-track{height:44px;}
  .s-chaos-card,.s-out-card{padding:8px 10px;}
}

@media (prefers-reduced-motion: reduce){
  .s-stage *,
  .s-stage *::before,
  .s-stage *::after{
    animation:none!important;
    transition:none!important;
  }

  .s-chaos-card,
  .s-out-card{
    opacity:1;
    transform:none;
  }

  .s-arr-track::after{
    display:none;
  }
}



___________________________________________________________________JAVASCRIPT________________________________________________________________________

<script>
(function () {
  function animCount(el, target, suffix, duration) {
    const start = performance.now();

    function step(now) {
      const p = Math.min((now - start) / duration, 1);
      const ease = 1 - Math.pow(1 - p, 3);
      el.textContent = Math.round(ease * target) + suffix;
      if (p < 1) requestAnimationFrame(step);
    }

    requestAnimationFrame(step);
  }

  let count = 1284;

  window.addEventListener('load', () => {
    const procEl = document.getElementById('proc-count');
    const m1 = document.getElementById('m1');
    const m2 = document.getElementById('m2');
    const m3 = document.getElementById('m3');

    if (!procEl || !m1 || !m2 || !m3) return;

    function tickCount() {
      count += Math.floor(Math.random() * 3) + 1;
      procEl.textContent = count.toLocaleString();
    }

    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
      m1.textContent = '2,400+';
      m2.textContent = '890+';
      m3.textContent = '5,100+';
      procEl.textContent = '1,284';
      return;
    }

    setTimeout(() => {
      animCount(m1, 2400, '+', 1800);
      animCount(m2, 890, '+', 1600);
      animCount(m3, 5100, '+', 2000);
      procEl.textContent = count.toLocaleString();
      setInterval(tickCount, 1800);
    }, 1600);
  });
})();
</script>

____________________________________________________________________________________________________________________